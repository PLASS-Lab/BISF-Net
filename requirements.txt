# BiSF-Net — core dependencies (tested versions)
# Install PyTorch matching your CUDA from https://pytorch.org first, e.g.:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
timm==1.0.22
numpy>=1.24
pillow>=10.0
tqdm>=4.65
